(function()
{
    return function(nexacro, system, trace, environment, application)
    {
        const init = {};
        init.Buttoniv_Button0 = function(obj)
        {
        	obj.set_background("aqua");
        };

        init.Tabiv_Tab0 = function(obj)
        {
        };

        init.TabpageControliv_Tabpage0 = function(obj)
        {
        	obj.set_text("Tabpage0000");
        };

        init.Layoutiv_Layout0 = function(obj)
        {
        };

        init.Formiv_Form0 = function(obj)
        {
        };

        init.Diviv_Div0 = function(obj)
        {
        };

        environment._setInitValues(Object.freeze(init));   
	}
}
)();
