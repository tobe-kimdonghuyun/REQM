//XJS=#{XJS_FILENAME}
(function()
{
    const fn = function(path, nexacro, system, trace, environment, application)
    {
        var obj;
    
        // User Script
#{USER_COMPILED_SCRIPT}
    
        this.loadIncludeScript(path);
        
        obj = null;
    };
    if (document.currentScript)
        document.currentScript.dispatchEvent(new CustomEvent("loadModule", { detail: fn }));

    return fn;
}
)();