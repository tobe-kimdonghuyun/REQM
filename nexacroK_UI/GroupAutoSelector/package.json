{
    "name": "GroupAutoSelector",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "nexacro-gen --project GroupAutoSelector.xprj --output \"_output_\"",
        "dev": "npm run build && wds --root-dir \"_output_\" --watch --open",
		"serve": "wds --root-dir \"_output_\" --watch --open"
    },
    "devDependencies": {
        "@web/dev-server": "^0.4.6",
        "@web/dev-server-hmr": "^0.4.1"
    }
}
